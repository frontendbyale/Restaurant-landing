---
const { image, title, description, price } = Astro.props;
---

<div class="bg-white dark:bg-gray-900 rounded-xl shadow p-4 text-left">
  <img src={image} alt={title} class="rounded-lg mb-4" loading="lazy" />
  <h3 class="text-xl font-semibold mb-2">{title}</h3>
  <p class="text-gray-600 dark:text-gray-400 mb-2">{description}</p>
  <span class="font-bold text-primary">{price}</span>
</div>